import { FC } from "react";
import { Typography } from "@mui/material";

interface FoodEditProps {}
const FoodEdit: FC<FoodEditProps> = () => {
  return <Typography>Food</Typography>;
};

export default FoodEdit;
